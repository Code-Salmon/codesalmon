
node fetch to add to index:

import fetch from 'node-fetch';

const response = await fetch('https://api.github.com/users/github');
const data = await response.json(); <--- copy/pasted from node-fetch doc

except the data is the url of the fetch call
to match this with code we can use a regex expression and the match method

I think this might be a problem if the user's data has variables or template literal passed into their fetch calls

apiURLGrab = code.match(/fetch\(['"](.+?)['"]/));
if(apiURLGrab) {
    const apiURL = apiURLMatch[1];
    // the word fetch would be the [0] the url is the [1]
}

try {
    const response = await fetch(apiURL)
    const data = await response.json();
    console.log(`Response data from ${apiURL}:`, data);
    fileFolder(data);
} catch (error) {
    console.error(`Error making test call to ${apiURL}:`, error)
}


console.log(data);